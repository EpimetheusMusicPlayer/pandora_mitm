import 'package:pandora_mitm/plugin_dev.dart';

/// A [PandoraMitm] plugin that does not do anything.
///
/// This plugin is useful in situations where a plugin is needed, but not
/// wanted.
class IdentityPlugin extends PandoraMitmPlugin {
  const IdentityPlugin();
}
